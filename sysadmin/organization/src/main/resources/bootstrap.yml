server:
  port: 8010

spring:
  application:
    name: organization
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:127.0.0.1}:${REGISTER_POST:8848}
      config:
        server-addr: ${REGISTER_HOST:127.0.0.1}:${REGISTER_POST:8848}
        file-extension: yml
