server:
  port: 8000

spring:
  application:
    name: authorization-server
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:127.0.0.1}:${REGISTER_POST:8848}
      config:
        server-addr: ${REGISTER_HOST:127.0.0.1}:${REGISTER_POST:8848}
        file-extension: yml
